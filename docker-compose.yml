services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=${SECRET_KEY:-change-this-in-production}
      - HOST=0.0.0.0
      - PORT=5000
    volumes:
      - canteen_data:/app/static/uploads
      - canteen_db:/app

volumes:
  canteen_data:
  canteen_db:
